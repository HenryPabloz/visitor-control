// Precisa bater com os valores de "name" cadastrados na tabela Role
// (veja prisma/seed.ts) — só existem esses dois papéis no sistema hoje.
export enum RoleEnum {
  RECEPTIONIST = 'RECEPTIONIST',
  ADMIN = 'ADMIN',
}
